# ── 环境变量模板 ──────────────────────────────────────
# GitHub Personal Access Token（提高 API 限额到 5000/h）
# 不带 Token 时限额为 60/h，建议设置
GITHUB_TOKEN=

# 定时调度器开关（true/false）
SCHEDULER_ENABLED=true

# API 服务地址和端口
API_HOST=0.0.0.0
API_PORT=8000
